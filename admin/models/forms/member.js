window.addEvent('domready', function() {
    document.formvalidator.setHandler('name',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('alias',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('picture',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('number',
		                      function (value) {
			                  regex=/^[0-9]+$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('position',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('residence',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('nicknames',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('dob',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('nationality',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('club_history',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('honours',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('about',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('quote',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('home_town',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('fave_player',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('height_weight',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('school_attending',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('graduating_class',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('sat_act',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('level_rating',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('decision_makers',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('travel_schedule',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('hobbies',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('conference',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('leaving_date',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('joining_date',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('first_name',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('middle_name',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
    document.formvalidator.setHandler('last_name',
		                      function (value) {
			                  regex=/^.*$/;
			                  return regex.test(value);
	                              });
});